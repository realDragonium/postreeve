services:
  postreeve:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      POSTREEVE_MASTER_KEY: ${POSTREEVE_MASTER_KEY:-}
    volumes:
      - postreeve-data:/app/data
    restart: unless-stopped

volumes:
  postreeve-data:
